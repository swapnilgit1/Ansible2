---
- name: module 1
  hosts: server1
  become: yes
  vars:
   pkg:         #pkg is dict type variable
    - vsftpd
    - tree
    - wget

  tasks:  
  - 
    name : install sw using loop
    apt: 
      name: "{{ item }}" 
      state: present
    with_items: "{{ pkg }}"